#!/usr/bin/env bash
set -euo pipefail

# Future purpose:
# - Run conservative LoRA fine-tuning experiments for OpenVLA.
# - Start with small debug runs, small data, batch_size=1, and short schedules.
#
# Current phase:
# - This placeholder does not load checkpoints.
# - It does not start training.
# - It must be completed only after explicit confirmation.

echo "run_lora_finetune.sh is a placeholder for future OpenVLA LoRA fine-tuning."
echo "No checkpoints were loaded and no training was started."
echo "TODO: add LoRA command only after GPU memory, data paths, and official examples are confirmed."
